# Архитектура: комбинаторная модель контента

> Живой документ. Здесь фиксируем продуктовые решения по архитектуре платформы,
> чтобы они не терялись между сессиями. Дополняется по мере обсуждения тем.
> Статус: в работе (обсуждаем тему «базовая структура»).

## Главный принцип — комбинаторика

Контент модульный. Преподаватель постоянно создаёт **блоки** (уроки) по мере того,
как сам узнаёт новое, — база растёт и идёт чуть впереди программ. Из этих блоков
**комбинируются** разные форматы: один блок → менторская, несколько → интенсив,
большая часть → курс.

Ключевая идея: **урок не принадлежит одному курсу.** Это переиспользуемый кирпич,
который входит в разные конструкции. Контент пишется один раз — и «перепродаётся»
сколько угодно раз через новые запуски.

Цитата заказчика, задающая смысл:
«я хочу чтобы это падало в копилку и я мог это перепродать ещё 10, 50, 100, тысячу раз».

## Три слоя

### 1. Урок (блок) — атом контента
- Видео, материалы, тезисы, шаблон задания.
- **Ничей**: не привязан к потоку. Лежит в общей копилке.
- Это растущая база знаний преподавателя.

### 2. Сборка (программа) — комбинация блоков
- Упорядоченный набор уроков + тип: **курс / интенсив / менторская**.
- Это «продукт», который продаётся и переиспользуется.
- Один урок входит в несколько сборок (связь многие-ко-многим, с порядком).

### 3. Поток — запуск сборки для группы
- Конкретный набор людей + чат + расписание.
- «Перепродать 100 раз» = 100 потоков одной сборки.
- Сборка пишется один раз, потоков под неё — сколько угодно.

### Под капотом: Занятие (сессия)
- Проведение конкретного урока внутри конкретного потока: дата, время, ссылка на
  созвон, статус, запись.
- Нужно потому, что один урок в разных потоках идёт в разные даты и со своей записью.
- Для преподавателя это просто «урок в расписании потока».

## Скелет сущностей (целевой)

```
Урок (Lesson)        контент, БЕЗ привязки к потоку (убираем streamId)
Сборка (Program)     name, type: курс | интенсив | менторская
СборкаУрок           program <-> lesson + порядок (многие-ко-многим)
Поток (Stream)       name, -> Сборка (опц.; у менторской нет), статус, группа, чат
Занятие (Session)    поток x урок: дата, время, ссылка, статус, запись
Запись студента      студент x задание в рамках потока
```

Отличие от текущей схемы: сейчас `Lesson.streamId` и `Assignment.streamId`
обязательны — контент одноразовый. В целевой модели уроки независимы, а даты
и записи переезжают на уровень «Занятия» (поток × урок).

## Как ложатся форматы заказчика

- **Большой курс** (Денис + Серёжа) — одна Сборка(курс); наборы студентов = потоки.
  Между наборами правится сама сборка/уроки.
- **Курс Серёжи** (дизайн) — отдельная Сборка(курс) со своими потоками. Независим.
- **Менторские** — поток БЕЗ фиксированной сборки: группа + занятия, добавляемые по
  ходу, у каждого свой свежий блок. Запись проведённого занятия падает в копилку как
  переиспользуемый блок (потом может войти в курс/интенсив). Оплата за каждое занятие.
- **Интенсив** — Сборка(интенсив) из нескольких блоков.

Всё собирается из одной общей копилки уроков.

## Уже принятые решения

- **Задания.** Одно задание на урок, раздаётся **всем** ученикам потока автоматически,
  каждый делает свою копию. Ручного назначения конкретному ученику нет — убираем.
- **Материалы.** Живут на уроках. Пункт меню «Материалы» — агрегатор: быстрый доступ
  ко всем материалам сразу за всё обучение. Отдельную «Библиотеку» не вводим.
  (Открытый нюанс: материалы «вообще по курсу», не привязанные к уроку — решить позже.)
- **Несколько преподавателей.** Общий двор: все админы видят всех студентов и все
  сборки, жёстких стен нет. У сборки и потока есть **ведущий** (владелец) — он питает
  фильтры «моё», умные умолчания (на дашборде первыми мои студенты и мои проверки;
  вопрос студента по умолчанию летит ведущему его потока) и атрибуцию. Сейчас двое
  равноправных совладельцев-админов (Денис, Серёжа). «Направление особняком»
  (дизайнеры Серёжи и т.п.) — это следствие фильтра по ведущему, а не изоляция.
  Урезанную роль «ассистент/проверяющий» откладываем до темы автоматизации.
- **Витрина (публичный фасад).** Публичная проекция платформы поверх данных, которые
  уже есть. Каждая сборка → карточка: название, тип, ведущий, блок «чему научишься»
  (новое маркетинговое поле), программа = заголовки уроков (без контента) и кейсы
  студентов. Публично: программа, результаты, кейсы. Под замком (после записи): видео
  и материалы уроков. Кейсы студентов показываем с **отбором преподавателя + согласием
  студента**. Маркетинговый текст можно переиспользовать из `landing/copy.md`.
- **Оплата и баланс.** Оплата внутри платформы через личный **баланс** студента:
  пополнил → тратит на менторские / программы / интенсивы. Привязка денег:
  курс/интенсив — разовая оплата за сборку; **менторская — оплата за каждое занятие**
  (люди ходят выборочно). Преподаватель централизованно видит «кто оплатил / кто нет»
  по каждому набору и по каждой менторской. Решает текущую боль: на менторские ходят
  ~20 человек, каждый платит сам, непонятно кто оплатил.

  Провайдер и поэтапность:
  - Провайдер пополнения — **ЮKassa** (у заказчика есть опыт подключения).
  - **Фаза 1 (с неё начинаем): ручное пополнение.** Студент присылает скриншот
    перевода через существующий чат с учеником; админ вручную начисляет баланс
    отдельным действием «Пополнить баланс» (сумма + заметка/пруф). НЕ заводим под это
    «задание» — деньги отдельно от домашки. Кошелёк, списания и отчёт «кто оплатил»
    работают одинаково независимо от источника пополнения.
  - **Фаза 2: авто-пополнение через ЮKassa** — поверх той же модели кошелька.
  - Модель — **баланс с разовыми списаниями**, не регулярная подписка (уточнить,
    если подписка всё же понадобится).
  - Возвраты, сгорание баланса, частичные доступы — позже.
  - Это самая крупная новая подсистема; на стройке ставим не первой.
- **Расписание и менторские.**
  - Регулярные серии/повтор НЕ делаем: каждая менторская — новый материал, занятие
    создаётся заново.
  - Менторская = группа (поток) + ad-hoc занятия во времени, у каждого свой свежий
    блок; фиксированной сборки нет (она у курса/интенсива). Продаётся за каждое занятие.
  - **«Мой день/неделя»** — экран-агенда преподавателя: мои занятия по моим потокам,
    кнопка «подключиться», рядом «кто оплатил/пришёл». Закрывает исходную боль
    «как выглядит моё воскресенье».
  - **Zoom по API** (фазируется, как оплата):
    - платформа создаёт встречу в Zoom и хранит ссылку → кнопка «подключиться» внутри;
    - после занятия запись авто-подтягивается из облака Zoom и прикрепляется;
    - **запись проведённой менторской авто-попадает в копилку как переиспользуемый
      блок** — замыкает петлю «провёл живое → упало в копилку → перепродал».
    - Фаза 1 (сейчас): руками — вставить ссылку, загрузить видео (платформа уже умеет).
    - Фаза 2: авто через Zoom API. Платный Zoom API с облачной записью у заказчика
      уже есть (использует) → интеграция не блокирована внешними зависимостями,
      можно не откладывать в самый конец. Нужно только приложение Server-to-Server OAuth.
    - Уточнение по записи: для менторской запись = рождение блока в копилке; для курса,
      который гоняется повторно, запись каждого набора прикрепляется к занятию набора
      (у каждого потока своя запись), сам блок не перезаписывается.

## Открытые темы (повестка, обсуждаем по одной)

1. Базовая структура (Урок / Сборка / Поток) — **согласовано, фиксируется этим документом**.
2. Несколько преподавателей — **согласовано** (общий двор + «ведущий»).
3. Студенты — запись, онбординг, масштаб. **В работе.** Запись через витрину;
   самообслуживание вместо ручного заведения. Анкету сохраняем и встраиваем во вход.
   Витрина и оплата/баланс — согласованы (см. «Уже принятые решения»).
4. Расписание и менторские — **согласовано** (без серий; агенда «мой день/неделя»;
   Zoom API с авто-записью в копилку, фазируется).
5. Материалы — добить нюанс с «общими по курсу».
6. Задания — добить остаток (механика согласована).
7. Проверка и фидбэк — каким должен быть разбор работы.
8. Прогресс студента — как видеть, где кто находится.
9. Автоматизация: API + Claude проверяет домашки (в самом конце).
