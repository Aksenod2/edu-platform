node_modules
.next
dist
.turbo
*.tsbuildinfo

.env
.env.local
.env.*.local

.git
.gitignore
.DS_Store
# *.md без ** якорится к корню контекста (README и т.п.) и НЕ матчит вложенные пути,
# но тексты юридических документов исключаем из игнора явно: они ОБЯЗАНЫ попадать
# в образ api — их читает сид (publishLegalVersions).
*.md
!packages/db/prisma/legal-content/*.md
