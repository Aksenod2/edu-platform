node_modules
dist
.next
.turbo
*.tsbuildinfo
next-env.d.ts

.env
.env.local
.env.*.local

.DS_Store
*.pem

npm-debug.log*
pnpm-debug.log*

storybook-static

# Playwright e2e
test-results
playwright-report
apps/web/e2e/.auth

.env.vps
.env.vps.bak.*
/deploy-config-backup/

# Эфемерные worktree субагентов (харнесс) — никогда не коммитим
.claude/worktrees/

# Локальный штамп гейта сборки (пишет post-build-stamp.sh) — не коммитим
.claude/.build-ok-stamp
.claude/settings.local.json
