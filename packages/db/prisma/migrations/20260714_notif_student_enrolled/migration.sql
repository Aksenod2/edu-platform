-- Новое значение enum NotificationType: «студент зачислен в группу» — уведомление
-- преподавателю/владельцу группы. Эпик «Уведомления преподавателю».
--
-- АДДИТИВНОСТЬ/БЕЗОПАСНОСТЬ: ALTER TYPE ... ADD VALUE — операция над метаданными типа,
-- без долгих локов и без трогания данных/строк. Новое значение в этой же миграции НЕ
-- используется (нет дефолтов/CHECK с ним), поэтому безопасно в транзакции `prisma migrate
-- deploy` (PostgreSQL 12+). Использование значения — в коде приложения (следующие задачи).
-- Размещаем в конце enum. Как в 20260701_notif_topup_requested / 20260705_mentorship_billing_enums.
ALTER TYPE "NotificationType" ADD VALUE 'student_enrolled';
