-- Признак «запись расписания создана/управляется из карточки урока».
-- Нужен, чтобы синхронизация «Даты занятия» урока трогала только свою запись,
-- а не записи, заведённые вручную через раздел «Расписание».
ALTER TABLE "ScheduleEntry" ADD COLUMN "managedByLesson" BOOLEAN NOT NULL DEFAULT false;
